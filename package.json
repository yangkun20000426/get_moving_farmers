{
  "name": "fitfarm",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gh-pages": "vite build && shx rm -rf docs && shx mkdir -p docs && shx cp -r dist/. docs/"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "shx": "^0.4.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.184.0",
    "vue": "^3.4.0"
  }
}
